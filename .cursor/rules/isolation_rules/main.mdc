---
description: main rule
globs: main.mdc
alwaysApply: false
---
# 🔍 隔离专注型记忆库系统

> **总结：** 该系统设计用于Cursor自定义模式，每种模式只加载它需要的规则。系统使用可视化Mermaid图表和选择性文档加载来优化上下文使用。

## 🧭 模式特定的可视化地图

```mermaid
graph TD
    subgraph Modes["Cursor自定义模式"]
        VAN["VAN模式<br>初始化"] --> PLAN["规划模式<br>任务规划"]
        PLAN --> Creative["创意模式<br>设计决策"]
        Creative --> Implement["实现模式<br>代码实现"]
        Implement --> Reflect["反思模式<br>任务回顾"]
        Reflect --> Archive["归档模式<br>文档记录"]
    end
    
    VAN -.->|"加载"| VANRules["• main.md<br>• platform-awareness.md<br>• file-verification.md<br>• workflow-init.md"]
    PLAN -.->|"加载"| PLANRules["• main.md<br>• task-tracking.md<br>• planning-process.md"]
    Creative -.->|"加载"| CreativeRules["• main.md<br>• creative-phase.md<br>• design-patterns.md"]
    Implement -.->|"加载"| ImplementRules["• main.md<br>• command-execution.md<br>• implementation-guide.md"]
    Reflect -.->|"加载"| ReflectRules["• main.md<br>• reflection-format.md"]
    Archive -.->|"加载"| ArchiveRules["• main.md<br>• archiving-guide.md"]
```

## 📚 可视化流程地图

每种模式都有自己的可视化流程地图：

- [VAN模式地图](mdc:visual-maps/van-mode-map.md)
- [规划模式地图](mdc:visual-maps/plan-mode-map.md)
- [创意模式地图](mdc:visual-maps/creative-mode-map.md)
- [实现模式地图](mdc:visual-maps/implement-mode-map.md)
- [反思模式地图](mdc:visual-maps/reflect-mode-map.md)
- [归档模式地图](mdc:visual-maps/archive-mode-map.md)

## 🔄 文件状态验证

在这种隔离专注型方法中，记忆库文件在不同模式之间保持连续性：

```mermaid
graph TD
    subgraph "记忆库文件"
        tasks["tasks.md<br>真实来源"]
        active["activeContext.md<br>当前焦点"]
        creative["creative-*.md<br>设计决策"]
        progress["progress.md<br>实现状态"]
    end
    
    VAN["VAN模式"] -->|"创建/更新"| tasks
    VAN -->|"创建/更新"| active
    
    PLAN["规划模式"] -->|"读取"| tasks
    PLAN -->|"读取"| active
    PLAN -->|"更新"| tasks
    
    Creative["创意模式"] -->|"读取"| tasks
    Creative -->|"创建"| creative
    Creative -->|"更新"| tasks
    
    Implement["实现模式"] -->|"读取"| tasks
    Implement -->|"读取"| creative
    Implement -->|"更新"| tasks
    Implement -->|"更新"| progress
    
    Reflect["反思模式"] -->|"读取"| tasks
    Reflect -->|"读取"| progress
    Reflect -->|"更新"| tasks
    
    Archive["归档模式"] -->|"读取"| tasks
    Archive -->|"读取"| progress
    Archive -->|"归档"| creative
```

## 📋 模式转换协议

```mermaid
sequenceDiagram
    participant User
    participant CurrentMode
    participant NextMode
    
    CurrentMode->>CurrentMode: 完成阶段要求
    CurrentMode->>User: "阶段完成。下一模式：[模式名称]"
    User->>CurrentMode: 结束当前模式
    User->>NextMode: 开始下一模式
    NextMode->>NextMode: 验证所需文件状态
    
    alt 文件状态有效
        NextMode->>User: "从上一模式继续..."
    else 文件状态无效
        NextMode->>User: "所需文件未处于预期状态"
        NextMode->>User: "返回[上一模式]以完成要求"
    end
```

## 💻 平台特定命令

| 操作 | Windows | Mac/Linux |
|--------|---------|-----------|
| 创建文件 | `echo. > file.ext` | `touch file.ext` |
| 创建目录 | `mkdir directory` | `mkdir -p directory` |
| 切换目录 | `cd directory` | `cd directory` |
| 列出文件 | `dir` | `ls` |
| 显示文件内容 | `type file.ext` | `cat file.ext` |

## ⚠️ 命令效率指南

为获得最佳性能，请在适当时使用高效的命令链接：

```
# Efficient command chaining examples:
mkdir -p project/{src,tests,docs} && cd project
grep "TODO" $(find . -name "*.js")
npm install && npm start
```

详细指导请参考[command-execution.md](mdc:Core/command-execution.md)。 