---
description: Visual process map for VAN mode file verification
globs: van-file-verification.mdc
alwaysApply: false
---
# VAN模式: 文件验证

> **简介:** 检查必要的Memory Bank和文档组件的存在性及基本结构。

## 📁 文件验证流程

```mermaid
graph TD
    FV["文件验证"] --> CheckFiles["检查必要文件"]
    CheckFiles --> CheckMB["检查Memory Bank<br>结构"]
    CheckMB --> MBExists{"Memory Bank<br>存在?"}
    
    MBExists -->|"是"| VerifyMB["验证Memory Bank<br>内容(基本)"]
    MBExists -->|"否"| CreateMB["创建Memory Bank<br>结构(基本)"]
    
    CheckFiles --> CheckDocs["检查文档<br>文件(例如tasks.md)"]
    CheckDocs --> DocsExist{"文档<br>存在?"}
    
    DocsExist -->|"是"| VerifyDocs["验证文档<br>存在性"]
    DocsExist -->|"否"| CreateDocs["创建必要<br>文档文件"]
    
    VerifyMB & CreateMB --> MBCP["Memory Bank<br>检查点"]
    VerifyDocs & CreateDocs --> DocsCP["文档<br>检查点"]
    
    MBCP & DocsCP --> FileComplete["文件验证<br>完成"]
    
    style FV fill:#4da6ff,stroke:#0066cc,color:white
    style FileComplete fill:#10b981,stroke:#059669,color:white
    style MBCP fill:#f6546a,stroke:#c30052,color:white
    style DocsCP fill:#f6546a,stroke:#c30052,color:white
```

## 📋 检查点验证模板(示例)

```
✓ 部分检查点: 文件验证
- Memory Bank目录已存在/已创建? [是/否]
- 必要文档(tasks.md)已存在/已创建? [是/否]

→ 如果全部为是: 文件验证完成。
→ 如果任何为否: 在继续前解决问题。
```

**下一步:** 加载并处理`van-complexity-determination.mdc`。 