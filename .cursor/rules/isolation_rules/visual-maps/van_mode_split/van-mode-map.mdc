---
description: Visual process map for VAN mode (Index/Entry Point)
globs: van-mode-map.mdc
alwaysApply: false
---
# VAN模式: 初始化流程图(索引)

> **简介:** 这是VAN模式的入口点。它处理初始激活并将流程引导到存储在单独文件中的后续步骤，以实现优化。

## 🚀 VAN模式激活

当用户输入"VAN"时，回应确认并开始流程:

```
用户: VAN

回应: OK VAN - 开始初始化流程
正在加载平台检测图...
```

## 🧭 VAN模式流程(高级视图)

此图显示了主要阶段。每个阶段在按顺序加载的单独文件中详细说明。

```mermaid
graph TD
    Start["开始VAN模式"] --> PlatformDetect["1. 平台检测
(van-platform-detection.mdc)"]
    PlatformDetect --> FileVerify["2. 文件验证
(van-file-verification.mdc)"]
    FileVerify --> Complexity["3. 早期复杂度
判断
(van-complexity-determination.mdc)"]
    Complexity --> Decision{"级别?"}
    Decision -- "级别1" --> L1Complete["级别1初始化完成"]
    Decision -- "级别2-4" --> ExitToPlan["退出到PLAN模式"]

    %% 链接到QA(单独加载)
    QA_Entry["VAN QA模式
(通过'VAN QA'命令
单独加载)"] -.-> QA_Map["(van-qa-validation.mdc)"]

    style PlatformDetect fill:#ccf,stroke:#333
    style FileVerify fill:#ccf,stroke:#333
    style Complexity fill:#ccf,stroke:#333
    style QA_Map fill:#fcc,stroke:#333
```

**下一步:** 加载并处理`van-platform-detection.mdc`。 